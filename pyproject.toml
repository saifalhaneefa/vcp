[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "vcp-backtester"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["numpy>=2.0","pandas>=2.2","pyyaml>=6.0"]

[tool.pytest.ini_options]
pythonpath = ["src"]
